<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>Login – Schulbibliothek</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap (für Responsive + Aufgabe) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <div class="wrap">
        <h1>Bibliotheks-Login</h1>

        <!-- Fehlermeldung bei falschen Login-Daten -->
        <% if (error) { %>
            <p style="color: red;"><%= error %></p>
        <% } %>

        <!-- Login-Formular: sendet an POST /login -->
        <form method="post" action="/login">
            <label>Benutzername</label>
            <input type="text" name="bename" required>

            <label>Passwort</label>
            <input type="password" name="passwort" required>

            <div class="actions" style="margin-top:10px;">
                <button type="submit" class="btn">Einloggen</button>
            </div>
        </form>

        <!-- Zurück zur öffentlichen Startseite -->
        <div class="actions" style="margin-top:10px;">
            <a href="/" class="btn btn-secondary">Zur Startseite</a>
        </div>
    </div>
</body>
</html>
