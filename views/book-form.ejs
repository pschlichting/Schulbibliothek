<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title><%= formTitle %> – Schulbibliothek</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap + eigenes CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <div class="wrap">
        <!-- Überschrift (kommt aus app.js: formTitle) -->
        <h1><%= formTitle %></h1>

        <!-- Formular für neues / bestehendes Buch -->
        <form method="post" action="<%= formAction %>">
            <!-- ISBN -->
            <label>ISBN</label>
            <input type="text" name="isbn" required value="<%= book ? book.isbn : '' %>">

            <!-- Titel -->
            <label>Titel</label>
            <input type="text" name="titel" required value="<%= book ? book.titel : '' %>">

            <!-- Beschreibung -->
            <label>Beschreibung</label>
            <textarea name="beschreibung" rows="3">
<%= book ? (book.beschreibung || '') : '' %>
            </textarea>

            <!-- Autor -->
            <label>Autor</label>
            <input type="text" name="autor" value="<%= book ? (book.autor || '') : '' %>">

            <!-- Verlag -->
            <label>Verlag</label>
            <input type="text" name="verlag" value="<%= book ? (book.verlag || '') : '' %>">

            <!-- Kategorie -->
            <label>Kategorie</label>
            <input type="text" name="kategorie" value="<%= book ? (book.kategorie || '') : '' %>">

            <!-- Anschaffungspreis -->
            <label>Anschaffungspreis (€)</label>
            <input type="number" step="0.01" name="apreis" value="<%= book ? (book.apreis || '') : '' %>">

            <!-- Gesamtanzahl Exemplare -->
            <label>Gesamtanzahl Exemplare</label>
            <input type="number" min="0" name="anzahlges" required
                   value="<%= book ? book.anzahlges : 1 %>">

            <!-- Verfügbare Exemplare nur anzeigen, wenn Buch schon existiert -->
            <% if (book) { %>
                <label>Verfügbare Exemplare</label>
                <input type="number" min="0" name="anzahlver" required value="<%= book.anzahlver %>">
            <% } else { %>
                <!-- bei neuem Buch keine separate Eingabe; wird in app.js auf anzahlges gesetzt -->
                <input type="hidden" name="anzahlver" value="">
            <% } %>

            <!-- Buttons: Speichern / Abbrechen -->
            <div class="actions">
                <button type="submit" class="btn">Speichern</button>
                <a href="/admin" class="btn btn-secondary">Abbrechen</a>
            </div>
        </form>
    </div>
</body>
</html>
